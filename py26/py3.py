'''memory mapped file (mmap)
    *memory mapped file is object representation
     of a file .
    *All operations are done one the memory
     mapped file within very less time.
    *we can deal with the file as we deal with
     the objects.
'''


